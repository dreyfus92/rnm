#!/usr/bin/env node
/**
 * rnm – interactive batch file renamer CLI
 * Entry: intro, path, find/replace prompts, preview, confirm, spinner, renamer, outro.
 */

// TODO: implement Clack flow
// - intro('rnm')
// - path({ message, directory: true, initialValue: process.cwd() })
// - select: match type (Literal | Regex)
// - text: find pattern, replace string
// - note: preview renames
// - confirm → spinner → renamer.apply() → outro

console.log("rnm – batch file renamer (stub)");
